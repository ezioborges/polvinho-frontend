import newElement from "../../utils/newElement.js";

const Subjects = () => {
    const subjectsContent = newElement('div')
    subjectsContent.textContent = 'Area para listagem do subjects'

    return subjectsContent
}

export default Subjects;