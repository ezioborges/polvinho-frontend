import newElement from '../../utils/newElement.js';

const QuizRegister = () => {
	const registerContent = newElement('div');
	registerContent.textContent = 'register content';

	return registerContent;
};

export default QuizRegister;
